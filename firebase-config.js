/* ============================================================
   Firebase project configuration
   ============================================================
   Replace every value below with your OWN Firebase project's
   config. Get it from:

   Firebase Console → (your project) → Project settings (gear icon)
   → scroll to "Your apps" → Web app → SDK setup and configuration

   It will look exactly like the object below, just with real
   values instead of placeholders. Copy-paste the whole thing.
   ============================================================ */

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
